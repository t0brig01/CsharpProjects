using System;
using System.Collections.Generic;
using System.Globalization;
using System.Threading;

namespace PEp5
{
    class Program
    {
        static void Main(string[] args)
        {
			//Did it on paper. Find the prime factor of every number 1-20 then put them in a list and multiply



	        /*int r;
	        int count = 0;
	        int answer;
	        for (int i = 1; i > 100000; i++)
	        {
		        for (int j = 1; j > 20; j++)
		        {
					
			        r = i % j;
			        if (r == 0)
			        {
				        ++count;
			        }
					Console.WriteLine("i = {0}   j = {1}   r = {2}",i,j,r);
		        }

		        if (count == 19)
		        {
			        answer = i;
					Console.WriteLine(answer);
		        }

		        count = 0;
	        }*/
	        Console.ReadKey();
        }
    }
}
